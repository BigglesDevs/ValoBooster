// config.example.js — TEMPLATE (safe to commit/share)
// Copy this to config.js and fill in your real values.
// config.js is git-ignored so your webhook URL stays private.

window.VBCONFIG = {
  discordWebhook: 'https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN',
};
